<!--
Calculates the fibonacci for the given value n.

Starting by n == 0 the sequence is [0, 1, 1, 2, 3, 5, 8, 13, ... ]
-->
fn fib(n:number): {

    if n == 0 {
        return 0
    }

    if n == 1 {
        return 1
    }

    return fib(n-1) + fib(n-2) # Compute fib by recursion
}