fn is-divisible(number:Int, divisor: Int) -> Bool:
    return number % divisor == 0
end

fn is-even(number: Int) -> Bool:
    return is-divisible(number, 2)
end

println("--- Odd/Even ---")
var i = 0
var odd-or-even: String
while i < 10:
    if is-even(i):
        odd-or-even = "even"
    else:
        odd-or-even = "odd"
    end

    println("The number " + str(i) + " is " + odd-or-even)
    i = i + 1
end
